import styled from 'styled-components';

const Header = () => {
  return (
    <Div id="header">
    </Div>
  );
};

const Div = styled.div`
  padding: 30px;
  border: 5px solid #000;
`;

export default Header;